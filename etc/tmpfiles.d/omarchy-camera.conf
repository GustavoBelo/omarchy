# The camera watcher can only watch for the disable flag once its directory
# exists, and omarchy-toggle-camera would otherwise create it on the first
# disable, after the bar is already watching.
d /var/lib/omarchy 0755 root root -
